{
  "name": "first",
  "version": "1.0.0",
  "description": "",
  "main": ".idea/dist/index(old).js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.5"
  }
}
